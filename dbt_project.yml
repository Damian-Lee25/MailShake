name: mailshake
version: "1.0"
config-version: 2

profile: mailshake

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  mailshake:
    staging:
      +materialized: view
      +schema: SILVER

    marts:
      +materialized: table
      +schema: GOLD
